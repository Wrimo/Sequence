take a, max
begin { -- expects a histories named a and max
    i <- 0
    current <- 9999999 -- think adding inf/ninf would be useful
}
expect i == #a {
    max <- current
    tprint(max)
}

if (a:i < current and a:i > max)  {
    current <- a:i
}

i <- i + 1